pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "La version de la aplicacion Docker es: ${env.DOCKER_APP_VERSION}"
            }
        }
    }
}
