pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "La versión de la aplicación Docker es: ${env.DOCKER_APP_VERSION}"
            }
        }
    }
}
